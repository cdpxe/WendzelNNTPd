set timeout 2

send_user "=== Test date command ===\n"

spawn telnet $env(nntp_address) 119

send_user "== Check response of date command ==\n"
send "date\n"
expect {
  timeout { send_user "Error: date command did not respond correctly\n"; exit 1 }
  -re "111 \\d{14}"
}
